package InterfaceExams;

public interface Compareable {
    //Return number less than 0 while A smaller than B,return 0 while A equals B,return number bigger than 0 while A bigger than B.
    int compareTo(Object other);
}